

@(Html.Kendo()
        .Upload()
        .Name("Image")
        .Multiple(true)
        .Async(async =>
        {
            async.AutoUpload(true)
                .Save("UploadedFiles", "TorrentsAdministration");
        }))
